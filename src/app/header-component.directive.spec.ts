import { HeaderComponentDirective } from './header-component.directive';

describe('HeaderComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderComponentDirective();
    expect(directive).toBeTruthy();
  });
});
